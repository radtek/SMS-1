@using Bec.TargetFramework.Web.Framework.Extensions
@using Ext.Net
@using Ext.Net.MVC
@model Bec.TargetFramework.Entities.ContactDTO

@(Html.X()
      .FormPanel()
      .ID("ContactForm")
      .Title("Contact")
      .AutoScroll(true)
      .ApplyFormPanelDefaults(Razor.ApplyFormPanelDefaults)
      
      .ItemsFromPartial("_ContactPartial",Model)
)