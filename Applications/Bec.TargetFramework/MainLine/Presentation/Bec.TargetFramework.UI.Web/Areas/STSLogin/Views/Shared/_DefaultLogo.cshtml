
@using Bec.TargetFramework.Web.Framework.Extensions
@using Ext.Net
@using Ext.Net.MVC
@model Bec.TargetFramework.Entities.vAttachmentDTO

@(Html.X().Panel().ID("OrganisationDefaultLogoPanel")
.Items(
Html.X().HiddenFor(model=> model.OrganisationID),
 Html.X().Image().ImageUrl("~/Content/resources/images/Default/logo-safe-move-scheme.png").Height(50).Width(200)
                    )
                    )
