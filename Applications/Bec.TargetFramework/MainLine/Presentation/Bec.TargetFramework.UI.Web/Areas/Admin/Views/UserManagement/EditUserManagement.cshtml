@using Bec.TargetFramework.Web.Framework.Extensions
@using Ext.Net
@using Ext.Net.MVC
@model Bec.TargetFramework.Entities.ContactDTO

@(Html.X().Panel().ID("EditUserManagementPanel")
.Items(Html.X().TabPanel().ID("UserEdit")
        .Items(
        Html.X().Panel()
        .Title("Edit User")
            .Items(
                            Html.X().Panel().ItemsFromPage(this, "~/Areas/Admin/Views/UserManagement/EditUser.cshtml")
                  )
            ,
          Html.X().Panel()
            .Title("Edit User Groups and Roles")
                .Items(
                                    Html.X().Panel().ItemsFromPage(this, "~/Areas/Admin/Views/UserManagement/AddUserGroupsRoles.cshtml", new { id = Model.ParentID, containerId = "AdministrationCenterPanel" })
                      )

     )))
