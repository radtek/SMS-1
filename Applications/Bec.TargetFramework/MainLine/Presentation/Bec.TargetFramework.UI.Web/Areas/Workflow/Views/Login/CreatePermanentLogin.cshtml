@model Bec.TargetFramework.Entities.Workflow.WorkflowInstanceCurrentStateDTO

@Html.TextBox("txtName", "Create Login")

@using (Html.BeginForm("CreatePermanentLogin", "Login", new { area = "Workflow" }))
{
    @Html.HiddenFor(model => model.CurrentActionDTO)
    <button name="next" type="submit">Next</button>
}
