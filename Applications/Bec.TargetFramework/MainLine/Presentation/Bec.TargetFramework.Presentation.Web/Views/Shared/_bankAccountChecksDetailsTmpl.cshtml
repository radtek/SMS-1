{{#each this}}
<div class="row">
    <div class="col-sm-12">
        <h3>{{Contact.Salutation}} {{Contact.FirstName}} {{Contact.LastName}} ({{SmsUserAccountOrganisationTransactionType.Description}})</h3>
    </div>
</div>
<div class="row">
    <div class="col-sm-8">
        {{#if SmsBankAccountChecks}}
        <h5>Bank Account Check Results</h5>
        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Account Number</th>
                        <th>Sort Code</th>
                        <th>Checked On</th>
                        <th>Result</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each SmsBankAccountChecks}}
                    <tr class="{{#if IsMatch}}match-bg{{else}}no-match-bg{{/if}}">
                        <td>{{BankAccountNumber}}</td>
                        <td>{{SortCode}}</td>
                        <td>{{CheckedOn}}</td>
                        <td>
                            {{#if IsMatch}}
                            Match
                            {{else}}
                            No Match
                            {{/if}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <p>The user is yet to use Safe Buyer.</p>
        {{/if}}
    </div>
    {{#if SmsSrcFundsBankAccounts}}
    <div class="col-sm-4">
        <h5>Source of Funds</h5>
        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Account Number</th>
                        <th>Sort Code</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each SmsSrcFundsBankAccounts}}
                    <tr>
                        <td>{{AccountNumber}}</td>
                        <td>{{SortCode}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    {{/if}}
    <div class="row">
        <div class="col-sm-12">
            <hr />
        </div>
    </div>
</div>
{{/each}}
