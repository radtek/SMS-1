@model string
@{
    ViewBag.Title = "Forgotten Username";
}
<div class="row">
    <div class="col-xs-12 col-sm-12 col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3">
        <form action="@Url.Action("Username", "Forgot", new { area = "Account"})" id="username-form" method="POST" class="client-form well no-padding">
            @Html.ValidationBootstrap()
            @Html.AntiForgeryToken()
            <header>
                <h1>Forgotten Username</h1>
            </header>
            <section class="fields">
                <fieldset>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group">
                                <label for="email">Please enter your registered email address</label>
                                <input type="email" name="email" class="form-control" />
                            </div>
                        </div>
                    </div>
                    <div class="row ">
                        <div class="col-sm-12">
                            <section class="captcha">
                                @Html.Partial("_Captcha")
                            </section>
                        </div>
                    </div>

                </fieldset>
            </section>
            <footer>
                <div class="row">
                    <div class="col-sm-offset-9 col-sm-3">
                        <button type="submit" id="formSubmit" class="btn btn-primary btn-block" disabled>
                            Continue
                        </button>
                    </div>
                </div>
            </footer>
        </form>
    </div>
</div>

@section Scripts {
    <script src="@Scripts.Url("~/Scripts/Account/Forgot/Username")"></script>
}