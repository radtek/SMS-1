@using Bec.TargetFramework.Entities
@using Bec.TargetFramework.Entities.Enums
@using Bec.TargetFramework.Infrastructure.Extensions
@using Bec.TargetFramework.Presentation.Web.Helpers

@model Edit<SmsTransactionDTO>
@{
    List<SelectListItem> titles = new List<SelectListItem>
    {
        new SelectListItem{Text = "Mr",Value="Mr"},
        new SelectListItem{Text = "Mrs",Value="Mrs"},
        new SelectListItem{Text = "Miss",Value="Miss"},
        new SelectListItem{Text = "Ms",Value="Ms"},
        new SelectListItem{Text = "Dr",Value="Dr"}
    };
}
<div class="modal fade in" tabindex="-1" role="dialog" aria-labelledby="editSmsTransactionLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="editSmsTransactionLabel">Edit Primary Buyer</h4>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-12">
                        <form action="@Url.Action("EditSmsTransaction", "Transaction", new { area = "SmsTransaction" })" method="POST" id="editSmsTransaction-form" class="smart-form">
                            @Html.ValidationBootstrap()
                            @Html.AntiForgeryToken()
                            @Html.Hidden("txID", (object)ViewBag.txID)
                            @Html.Hidden("uaoID", Model.Model.UserAccountOrganisationID)
                            @Html.HiddenFor(m => m.Model.UserAccountOrganisation.Contact.RowVersion)
                            @Html.HiddenFor(m => m.Model.UserAccountOrganisation.UserAccount.RowVersion)
                            <fieldset>
                                <section>
                                    <label class="label">Title</label>
                                    <label class="select">
                                        @Html.DropDownListFor(m => m.Model.UserAccountOrganisation.Contact.Salutation, new SelectList(titles, "Value", "Text"), "Please Select", new { tabindex = "1" })
                                        <i></i>
                                    </label>
                                </section>
                                <section>
                                    <label class="label">First Name</label>
                                    <label class="input ">
                                        <i class="icon-append fa fa-asterisk" style="font-size:0.7em;"></i>
                                        @Html.TextBoxFor(m => m.Model.UserAccountOrganisation.Contact.FirstName, new { tabindex = "2", maxlength = 50 })
                                    </label>
                                </section>
                                <section>
                                    <label class="label">Last Name</label>
                                    <label class="input">
                                        <i class="icon-append fa fa-asterisk" style="font-size: 0.7em;"></i>
                                        @Html.TextBoxFor(m => m.Model.UserAccountOrganisation.Contact.LastName, new {tabindex = "3", maxlength = 50})
                                    </label>
                                </section>
                                @if (ViewBag.IsTemporaryUser)
                                {
                                    <section>
                                        <label class="label">Email Address</label>
                                        <label class="input ">
                                            <i class="icon-append fa fa-asterisk" style="font-size:0.7em;"></i>
                                            @Html.TextBoxFor(m => m.Model.UserAccountOrganisation.UserAccount.Email, new { tabindex = "4", type = "email", data_url = Url.Action("CheckEmail", "Home", new { Area = "" }) })
                                        </label>
                                    </section>
                                }
                            </fieldset>
                        </form>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" id="cancelEditSmsTransaction" class="btn btn-default" data-dismiss="modal">
                    Cancel
                </button>
                <button type="button" id="submitEditSmsTransaction" class="btn btn-primary" tabindex="5">
                    Save
                </button>
            </div>
        </div>
    </div>
</div>

<script async src="@Scripts.Url("~/Scripts/SmsTransaction/Shared/_EditSmsTransaction")"></script>
